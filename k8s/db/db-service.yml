# https://kubernetes.io/docs/concepts/services-networking/service/
apiVersion: v1
kind: Service
metadata:
  name: db-service
spec:
  selector:
    app: databse-deployment
  type: ClusterIP
  ports:
    - protocol: "TCP"
      port: 3306
      targetPort: 3306
---
